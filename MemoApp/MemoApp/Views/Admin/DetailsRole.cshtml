@model RoleViewModel
@{
    Layout = null;
}
<form>
    <fieldset disabled>
        <div class="form-group">
            <label asp-for="Id">Role id</label>
            <input asp-for="Id" class="form-control" />
        </div>
        <div class="form-group">
            <label asp-for="Name">Role name</label>
            <input asp-for="Name" class="form-control" />
        </div>
    </fieldset>
    <button data-dismiss="modal" class="btn btn-primary"><i class="fa fa-list"></i> Back to roles</button>
</form>
